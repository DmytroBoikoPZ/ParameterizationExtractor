<mah:MetroWindow x:Class="Quipu.ParameterizationExtractor.Desktop.MainWindow"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:extras="clr-namespace:Quipu.ParameterizationExtractor.Desktop.Views.Extras"
                 xmlns:graph="clr-namespace:Quipu.ParameterizationExtractor.Desktop.Views.Graph"
                 xmlns:overview="clr-namespace:Quipu.ParameterizationExtractor.Desktop.Views.Overview"
                 xmlns:seed="clr-namespace:Quipu.ParameterizationExtractor.Desktop.Views.Seed"
                 xmlns:welcome="clr-namespace:Quipu.ParameterizationExtractor.Desktop.Views.Welcome"
                 Title="{Binding WindowTitle}"
                 Height="700" Width="1100">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Menu -->
    <Menu Grid.Row="0">
      <MenuItem Header="_File">
        <MenuItem Header="_New workspace..." Command="{Binding NewWorkspaceCommand}"/>
        <MenuItem Header="_Open workspace..." Command="{Binding OpenCommand}"/>
        <MenuItem Header="_Recent" ItemsSource="{Binding Welcome.Recents}">
          <MenuItem.ItemContainerStyle>
            <Style TargetType="MenuItem">
              <Setter Property="Header" Value="{Binding Path}"/>
              <Setter Property="Command" Value="{Binding DataContext.Welcome.OpenRecentCommand, RelativeSource={RelativeSource AncestorType=Menu}}"/>
              <Setter Property="CommandParameter" Value="{Binding Path}"/>
            </Style>
          </MenuItem.ItemContainerStyle>
        </MenuItem>
        <Separator/>
        <MenuItem Header="_Close workspace" Command="{Binding CloseCommand}"/>
        <Separator/>
        <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
      </MenuItem>
    </Menu>

    <!-- Toolbar (workspace-loaded only) -->
    <Border Grid.Row="1"
            BorderBrush="#22000000"
            BorderThickness="0,0,0,1"
            Visibility="{Binding IsWorkspaceLoaded, Converter={StaticResource BoolToVis}}">
      <StackPanel Orientation="Horizontal" Margin="8">
        <Button Content="Run"     IsEnabled="False" Padding="10,4" Margin="0,0,4,0"/>
        <Button Content="Dry-run" IsEnabled="False" Padding="10,4" Margin="0,0,4,0"/>
        <Button Content="Save"    IsEnabled="False" Padding="10,4" Margin="0,0,16,0"/>
        <Button Command="{Binding EditConnectionCommand}"
                ToolTip="Edit connection"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Cursor="Hand">
          <TextBlock Text="{Binding ConnectionDisplay}"
                     VerticalAlignment="Center"
                     FontStyle="Italic"
                     Opacity="0.75"/>
        </Button>
      </StackPanel>
    </Border>

    <!-- Content swap -->
    <Grid Grid.Row="2">
      <welcome:WelcomeView DataContext="{Binding Welcome}"
                           Visibility="{Binding DataContext.IsWorkspaceLoaded, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBoolToVis}}"/>

      <TabControl Visibility="{Binding IsWorkspaceLoaded, Converter={StaticResource BoolToVis}}">
        <TabItem Header="Overview">
          <overview:OverviewView DataContext="{Binding Overview}"/>
        </TabItem>
        <TabItem Header="Seed">
          <seed:SeedView DataContext="{Binding Seed}"/>
        </TabItem>
        <TabItem Header="Graph">
          <graph:GraphView DataContext="{Binding Graph}"/>
        </TabItem>
        <TabItem Header="Extras">
          <extras:ExtrasView DataContext="{Binding Extras}"/>
        </TabItem>
        <TabItem Header="Run">
          <TextBlock Margin="20" Opacity="0.7" Text="Run tab — implemented in desktop-dry-run-and-execute"/>
        </TabItem>
      </TabControl>
    </Grid>

    <!-- Status bar (workspace-loaded only) -->
    <Border Grid.Row="3"
            BorderBrush="#22000000"
            BorderThickness="0,1,0,0"
            Visibility="{Binding IsWorkspaceLoaded, Converter={StaticResource BoolToVis}}">
      <StackPanel Orientation="Horizontal" Margin="8,4">
        <TextBlock Text="last save: " Opacity="0.6"/>
        <TextBlock Text="{Binding LastSavedDisplay}" Opacity="0.75"/>
      </StackPanel>
    </Border>
  </Grid>
</mah:MetroWindow>
